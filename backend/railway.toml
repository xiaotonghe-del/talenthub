[build]
builder = "NIXPACKS"
buildCommand = "npm install && npx prisma generate"

[deploy]
startCommand = "npx prisma db push --accept-data-loss || true; node server.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
